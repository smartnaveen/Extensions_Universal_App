//
//  Closure.swift
//  Extension
//
//  Created by Mr. Naveen Kumar on 08/06/21.
//

import Foundation
import UIKit

// MARK: - Cell Class

/*
 class MyTableViewCell: UITableViewCell {
     typealias CompletionHandler = (_ value: Int) -> Void
     var completion: CompletionHandler?
     
     
     
     // Button Action
     @IBAction func buttonTap(_ sender: UIButton) {
         if let safeCompletion = completion {
             safeCompletion(sender.tag)
         }
     }
     
 }
 */


// MARK: - Controller Class
/*
 func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
     let cell = tableView.dequeueReusableCell(withIdentifier: "UserDetailCell", for: indexPath) as! UserTableViewCell
     
     cell.button.tag = indexPath.row
     cell.completion = { value in
         print(value)
     }
     
 */
